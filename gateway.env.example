# Gateway Environment Configuration
# This file demonstrates the XDG path configuration for different deployment modes

# ==============================================================================
# DEPLOYMENT MODE A: Off-Tinsnip (Standalone)
# ==============================================================================
# When deploying without tinsnip, use standard XDG paths in user home:

XDG_DATA_HOME=~/.local/share
XDG_CONFIG_HOME=~/.config  
XDG_STATE_HOME=~/.local/state

# ==============================================================================
# DEPLOYMENT MODE B: Via-Tinsnip
# ==============================================================================
# When deploying through tinsnip, paths point to NFS mounts:
# (These values would be set automatically by tinsnip setup.sh)

# XDG_DATA_HOME=/mnt/tinsnip/data
# XDG_CONFIG_HOME=/mnt/tinsnip/config
# XDG_STATE_HOME=/mnt/tinsnip/state

# ==============================================================================
# Common Configuration (same for both modes)
# ==============================================================================

# Service identification
TIN_NAMESPACE=dynamicalsystem
TIN_SERVICE_NAME=gateway
ENV_FILE=gateway.env

# OCI API Credentials
OCI_TENANCY_OCID=ocid1.tenancy.oc1..aaaaaaaaXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
OCI_USER_OCID=ocid1.user.oc1..aaaaaaaaXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
OCI_FINGERPRINT=XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX
OCI_REGION=us-phoenix-1
OCI_COMPARTMENT_ID=ocid1.compartment.oc1..aaaaaaaaXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
OCI_AVAILABILITY_DOMAIN=XXXX:US-PHOENIX-1-AD-1

# Note: Secret paths are now hardcoded in docker-compose.yml as:
# - /secrets/oci_api_key.pem (mounted from ${XDG_DATA_HOME}/${TIN_NAMESPACE}/${TIN_SERVICE_NAME}/oci/)
# - /secrets/id_oci.pub (mounted from ${XDG_CONFIG_HOME}/${TIN_NAMESPACE}/${TIN_SERVICE_NAME}/ssh/)

# Docker image configuration
GITHUB_REPOSITORY=dynamicalsystem/gateway
IMAGE_TAG=latest

# User/Group IDs (for off-tinsnip mode)
USER_ID=1000
GROUP_ID=1000

# ==============================================================================
# Tinsnip-specific variables (only used in tinsnip mode)
# ==============================================================================
# These are provided by tinsnip infrastructure:

# TIN_SERVICE_ENVIRONMENT=prod
# TIN_SERVICE_UID=11400
# PRIMARY_PORT=11400