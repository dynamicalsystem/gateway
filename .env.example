# Gateway Service Configuration
# Copy this to the appropriate XDG location: ~/.config/dynamicalsystem/gateway.prod.env
# Fill in your cloud provider credentials and configuration

# Optional: Override default paths (set these FIRST if you need custom paths)
# XDG_DATA_HOME=${HOME}/.local/share
# XDG_CONFIG_HOME=${HOME}/.config
# XDG_STATE_HOME=${HOME}/.local/state
# TIN_NAMESPACE=dynamicalsystem
# TIN_SERVICE_NAME=gateway
# TIN_ENVIRONMENT=prod

# Cloud Provider API Credentials (required)
OCI_TENANCY_OCID=ocid1.tenancy.oc1..aaaaaaaaa...
OCI_USER_OCID=ocid1.user.oc1..aaaaaaaaa...
OCI_FINGERPRINT=aa:bb:cc:dd:ee:ff:00:11:22:33:44:55:66:77:88:99
OCI_REGION=uk-london-1

# Resource compartment (use tenancy OCID for root compartment)
OCI_COMPARTMENT_ID=ocid1.tenancy.oc1..aaaaaaaaa...

# Availability domain (get this from: oci iam availability-domain list)
OCI_AVAILABILITY_DOMAIN=Iocq:UK-LONDON-1-AD-1

# Optional: Override file paths
# SSH public key file path for instance access
# Default: ~/.ssh/id_oci.pub
# OCI_SSH_PUBLIC_KEY_PATH=/path/to/your/ssh/public/key.pub

# Private key path
# Default: ${XDG_DATA_HOME}/${TIN_NAMESPACE}/${TIN_SERVICE_NAME}/oci/oci_api_key.pem
# OCI_PRIVATE_KEY_PATH=/path/to/oci_api_key.pem

# Optional: Deployment mode
# USE_RESOURCE_MANAGER=true    # Use OCI Resource Manager (legacy)
# Default: false (uses Terraform)